var navigationParameters = {
    links: [],
    search: {
        enabled: true,
        method: "JavaScript",
        javascriptAction: "SearchFilter($(this).val().toLowerCase(), 'Search')"
    },
    brandingLink: {
        url: "/Apps/RunRequest"
    }
}
